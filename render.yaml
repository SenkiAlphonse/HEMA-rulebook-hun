services:
  - type: web
    name: hema-rulebook-search
    runtime: python
    runtimeVersion: "3.11"
    buildCommand: |
      pip install -r requirements.txt && \
      pytest tests/ -v && \
      mkdir -p dist && \
      python build.py && \
      ls -la dist/rulebook.html
    startCommand: gunicorn --bind 0.0.0.0:${PORT:-10000} --workers ${WEB_CONCURRENCY:-1} app:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
      - key: FLASK_ENV
        value: production
